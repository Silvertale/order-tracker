var app = angular.module("create_app", []);

app.controller("create_controller", function($scope){
  //look at package for show basePrice
  //Show only the number of tiers we are using
  //pre-assign tier sizes by r_people
  //...
});